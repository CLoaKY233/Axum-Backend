# `rts` Module

This module defines the application's routes.

## File Structure

- `mod.rs`: The module's root, which exports the public API.
- `health.rs`: Defines the health check route.
- `root.rs`: Defines the root route.
- `sshconnect.rs`: Defines the SSH connection testing route.
