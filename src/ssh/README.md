# `ssh` Module

This module provides all SSH-related functionality.

## File Structure

- `mod.rs`: The module's root, which exports the public API.
- `connector.rs`: Handles the SSH connection.
- `error.rs`: Defines the SSH-specific error types.
- `models.rs`: Defines the SSH models and configurations.
