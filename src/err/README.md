# `err` Module

This module provides application-wide error handling.

## File Structure

- `mod.rs`: The module's root, which exports the public API.
- `error.rs`: Defines the main `AppError` enum and its implementations.
