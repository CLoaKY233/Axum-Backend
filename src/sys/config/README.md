# `config` Module

This module defines the application's configuration.

## File Structure

- `mod.rs`: The module's root, which exports the public API.
- `server.rs`: Defines the server configuration.
- `state.rs`: Defines the application state.
