# `env` Module

This module provides utilities for reading and parsing environment variables.

## File Structure

- `mod.rs`: The module's root, which exports the public API.
- `error.rs`: Defines the error types for environment variable handling.
- `loader.rs`: Provides functions for loading and parsing environment variables.
